# 任务清单（Task Backlog）

> 版本：v1.0 · 2026-08-13
> 按里程碑组织。ID 规则：`M{里程碑}-{序号}`。状态：☐ 未开始 / ◐ 进行中 / ☑ 完成。
> 依赖列仅标关键路径依赖。估时为单人日。

## M0 · POC（1 周）——目标：技术路线定论

| ID | 任务 | 估时 | 依赖 | 状态 |
|---|---|---|---|---|
| M0-1 | 工程初始化：uv 项目、依赖矩阵锁定（deepagents 0.7.5 / langgraph 1.2.11 / langchain-mcp-adapters 0.3.2 / langgraph-checkpoint-postgres 3.1.2）、目录骨架 | 0.5 | — | ☑ 2026-08-13 |
| M0-2 | LLM 接通：阿里 token plan 端点（qwen3.7-plus，与 deerflow config.yaml 对齐），function calling + 并行工具调用实测通过 | 0.5 | M0-1 | ☑ 2026-08-13 |
| M0-3 | MultiServerMCPClient 接入域验证：8201 调度域工具发现(6 工具)+真实调用(get_drone_status 返回现网数据)+healthz 跳过失败域，全部实测通过 | 0.5 | M0-1 | ☑ 2026-08-13 |
| M0-4 | create_deep_agent 最小组装 + 真实链路：查图斑→选机→汇报全程真实数据；图斑为 0 时诚实停止不硬派（质量正面信号） | 1 | M0-2, M0-3 | ☑ 2026-08-13 |
| M0-5 | interrupt_on 打通：dispatch_drone 触发 interrupt（用户说"不用确认"也拦得住）→ reject 后如实汇报未执行；决策格式 approve/edit/reject/respond | 0.5 | M0-4 | ☑ 2026-08-13 |
| M0-6 | **Qwen 达标验证** ☑：11 域 74 工具全量挂载下抽样 10/10 命中（=DeerFlow 基线），含注入反向用例 #71；并行调用与多步链路已在 M0-2/M0-5 验证。**决策：qwen3.7-plus 达标，暂不启用混合路由**；全量 76 条回归留 M1-12 | 1 | M0-4 | ☑ 2026-08-13 |
| M0-7 | **Aegra 评估** ☑：资料评审通过（Apache 2.0/1.1k stars/活跃/Agent Protocol/官方支持 Agent Chat UI）。**决策：生产首选 Aegra 裁剪，自建兜底**；hands-on 部署验证移 M1-7 首项 | 1 | M0-4 | ☑ 2026-08-13 |
| M0-8 | 开发环境 ☑：`langgraph dev --no-reload` + agent-chat-ui 浏览器实测全链路（UI 问禁飞区→list_zones 渲染→真实数据回答，截图留证） | 0.5 | M0-4 | ☑ 2026-08-13 |
| M0-9 | POC 结论文档 ☑：见 poc/M0结论.md（两大决策 + 实测清单 + 踩坑记录） | 0.5 | M0-6, M0-7 | ☑ 2026-08-13 |

**验收**：抽测命中率 ≥ DeerFlow 现状；并行派单无格式错误；两项决策落纸。

## M1 · 核心框架（3 周）——目标：11 域全接 + 生产端点 + 可观测

| ID | 任务 | 估时 | 依赖 | 状态 |
|---|---|---|---|---|
| M1-1 | Postgres 部署（含 pgvector）+ checkpointer/store 接入 | 1 | M0-9 | ◐ Aegra 栈已带 pgvector:pg18 容器，服务端 checkpointer/store 均 connected；剩余：容器持久化配置固化 + 我方 /memories StoreBackend 接入 |
| M1-2 | MCP 配置生成器 ☑：self_agent.nacos_sync 现网 Nacos 实测（登录/list/detail/探活/对账全通）；**修正原桥语义缺陷：listed-但探活不通保持现状，仅 registry 下线才摘除**（IP 漂移不再清空配置）；键名统一 registry 名；单测 3 条 | 2 | M0-3 | ☑ 2026-08-13 |
| M1-3 | 11 域全量接入 ☑（工具分层挂载：Lead 全量在 M0-6 实测无稀释，暂不分层，评测回归把关） | 1.5 | M1-2 | ☑ 2026-08-13 |
| M1-4 | 子代理配置化 ☑：config/subagents.json（uav-ops 18 工具/monitor 11/data-analyst 9，迁移自 deploy/config.yaml.example 并扩展），并行派单实测通过 | 1.5 | M1-3 | ☑ 2026-08-13 |
| M1-5 | 审计中间件 ☑：AuditMiddleware 落 Postgres self_agent_audit（库故障降级 JSONL 不阻断）；Lead 与各子代理逐个挂载（子代理独立中间件栈），实测子代理内部工具调用入表 | 1 | M1-1 | ☑ 2026-08-13 |
| M1-6 | 模型路由中间件（档位策略 + M0 决策的混合路由） | 1 | M1-4 | ☐ |
| M1-7 | 生产协议端点 ☑ **hands-on 验证通过，定论：引入 Aegra**（aegra-api 0.10.0 装入本 venv 零依赖冲突；aegra.json 指向 graph_entry.py；Postgres+Redis 容器；/health 三项 connected；curl 协议实测 + agent-chat-ui 直连实测均通过，任务队列/cron 自带） | 5~7 | M0-7, M1-1 | ☑ 2026-08-13 |
| M1-8 | 重启恢复 ☑（Aegra 基建承担 worker 职责）：interrupt 持久化 → kill 服务 → 重启 → 跨进程 resume 全链路实测通过 | 2 | M1-7 | ☑ 2026-08-13 |
| M1-9 | agent-chat-ui 接生产端点 ☑：直连 Aegra(:2027) 浏览器实测（threads 列表互通 + 工具调用渲染） | 1 | M1-7 | ☑ 2026-08-13 |
| M1-10 | Langfuse 可观测 ☑：自托管 v4 栈（端口重映射 override）+ **Aegra 原生 OTel target**（教训：graph.with_config 挂 callback 会破坏 checkpointer 注入，必须走服务端 OTel）；完整调用树入 ClickHouse（root/LLM/工具/中间件 span 全有）；注意 v4 下旧版 /api/public/traces 读 legacy 表为空，看 UI（:3001）或 events_core | 1.5 | M1-5 | ☑ 2026-08-13 |
| M1-11 | 记忆 ☑：CompositeBackend（默认 StateBackend + /memories→StoreBackend）；跨会话偏好写读实测通过（namespace 暂全局，R19 后按用户隔离） | 1 | M1-1 | ☑ 2026-08-13 |
| M1-12 | 评测回归 ☑：76 条全量首跑 70/76；6 条失败逐条归因修复——runner 缺 store（#8）、emergency_stop/return_home 误挂 interrupt 违反紧急白名单设计（#69/70）、提示词微调（#36/63/75 点名操作直接执行）；失败集复测 **6/6 全过，等效 76/76 = 基线持平** | 1.5 | M1-3 | ☑ 2026-08-13 |

**验收**：76 条评测 ≥ 现状；杀进程重启 run 续跑演示；任一 run 在 Langfuse 可查完整 trace。

## M2 · 交付物 + 入口（3.5 周）——目标：全链路场景可用

| ID | 任务 | 估时 | 依赖 | 状态 |
|---|---|---|---|---|
| M2-1 | P0 沙箱 ☑：WorkspaceShellBackend（LocalShellBackend + 虚拟路径改写 /skills·/work→相对路径）；受限 PATH（venv）+ 不继承环境 + 180s 超时；实测抓到并修复两问题：execute 虚拟路径不通、agent 内联绕过脚本写 /tmp（SKILL.md 加硬性纪律）。已知边界：shell 无隔离 + 工作区全会话共享，P1 Docker 解决 | 1 | M1 | ☑ 2026-08-13 |
| M2-2 | 文档四技能 ☑：pptx/docx/xlsx/pdf（统一 JSON 规格→脚本生成，PDF 自动注册中文字体）；脚本单测 4/4；e2e 实测「查图斑+告警→巡检汇报 PPT」产出 7 页真实数据 PPT 到工作区 | 4 | M2-1 | ☑ 2026-08-13 |
| M2-3 | 技能管理后端 ☑：skill_registry 表 + zip 导入（zip-slip 防护、20MB 上限）+ GitHub URL@ref 导入（整仓扫描多技能、pinned_ref 记录）+ CLI；**实测从本项目 GitHub 仓库导入 4 技能全流程通过** | 2 | M1-1 | ☑ 2026-08-13 |
| M2-4 | 技能安全流水线 ☑：frontmatter 结构校验 + 静态扫描（脚本 9 类危险模式 + SKILL.md 3 类注入模式，含行号定位）+ 默认 pending 人工审核（enable/reject/disable）；单测覆盖恶意样本 | 1.5 | M2-3 | ☑ 2026-08-13 |
| M2-5 | 会话装配 ☑：materialize 按 enabled 集合物化 /skills/managed/，新会话即时生效；禁用即刻从装配中消失（实测）；REST API 与管理页归 M2-12 | 1 | M2-3 | ☑ 2026-08-13 |
| M2-6 | 知识库 MVP ☑：pdf/docx/md 解析分块入库 + search_knowledge 工具 + CLI；检索双模式（EMBEDDING_* 配置→pgvector；当前 token plan 无 embedding 模型→pg_trgm word_similarity + 中文 2-gram 词项覆盖率，5/5 语义查询命中，配好 embedding 后 reindex 即升级）；e2e：规范+实时气象对照判定禁飞并引用条款 | 2.5 | M1-1 | ☑ 2026-08-13 |
| M2-7 | 企业微信通道 ◐：网关（8400）+ 渠道抽象（verify/parse/send 契约）就绪，企微适配器为骨架——**待 CorpID/Secret/Token/AESKey 凭据后按契约补三个函数即可** | 2.5 | M1-8 | ◐ |
| M2-8 | 钉钉通道 ◐：适配器已实现（HMAC-SHA256 验签含时间窗、sessionWebhook 回推、消息解析），单测 3 条通过；**待 DINGTALK_APP_SECRET 真机联调** | 1.5 | M2-7 | ◐ 代码就绪 |
| M2-9 | 确认卡片闭环 ☑：interrupt → 审批单落库 + 卡片回推（含动作参数/单号/口令说明）→ 「同意/拒绝 N」口令或 POST /approvals/{id}/decision → resume → 结果回推；**local 通道全链路实测**（含防重放：已决策单再决策被拒） | 2 | M2-7, M1-8 | ☑ 2026-08-13 |
| M2-10 | 统一身份 ☑：user_identity 表（渠道用户自动登记映射 user_id）+ 审批 decided_by 追责到人 | 1 | M2-7 | ☑ 2026-08-13 |
| M2-11 | 前端二开 ☑：webui 纳入版本管理；WorkPanel（计划=stream todos 实时 / 文件=网关 API 可下载）右侧抽屉；**Web 端 interrupt 审批 UI 为 agent-chat-ui 原生 agent-inbox 开箱即用**（Approve/Reject 表单浏览器实测）；子代理活动流依托原生工具调用渲染 | 3 | M1-9 | ☑ 2026-08-13 |
| M2-12 | 管理台 ☑：网关直出单页应用（/admin）——技能（GitHub/zip 导入、启停、审核、扫描发现查看）/ 知识库（上传、删除、检索测试）/ 审批（批准/拒绝按钮 + 8s 轮询）/ 工作区文件（下载）；REST API 全套 + CORS；浏览器实测 | 2.5 | M2-3, M2-6 | ☑ 2026-08-13 |
| M2-13 | 端到端验收 ☑ 两条路径：① 复合场景（查图斑+告警→安排核查→周报PPT）——agent 先飞前检查发现航线穿越真实禁飞区（汉川风机-14/12）诚实拒派但完成其余任务并交付 5 页 PPT；② **双层人在环全链路**：卡片同意→框架放行→服务端登记待确认单→网关管理端换 confirm_token（审批人留痕 ACT-0001）→edit 决策注入 token→派机成功 DSP-0001。过程中发现并修复 mcp-services 真实缺陷（drone_dispatch.py:73 confirm_token 误传给 attach_confirm_card 且未进核心调用） | 1 | 全部 | ☑ 2026-08-13 |

**验收**：全链路场景演示通过；GitHub 导入技能→审核→可用 ≤5 分钟；上传规程文档后对话正确引用。

## M3 · 生产化（2.5 周）——目标：可部署、可运营

| ID | 任务 | 估时 | 依赖 | 状态 |
|---|---|---|---|---|
| M3-1 | P1 沙箱：自研 Docker SandboxBackendProtocolV2（或自托管 Daytona 集成，二选一按 M2 复盘定） | 4 | M2-1 | ☐ |
| M3-2 | Playwright MCP ☑：stdio 接入（browser_* 24 工具）+ research 子代理场景实测（task 派单→浏览器打开页面→正确提取标题与正文） | 1 | M1-3 | ☑ 2026-08-13 |
| M3-3 | Playbook ☑：playbook 表 + CRUD/按模板发起 API + 参数槽位渲染（{region} 实测注入）+ 管理台模板任务页；投递到任意通道会话 | 2.5 | M1-8 | ☑ 2026-08-13 |
| M3-4 | 定时任务 ☑：网关 cron 轮询（30s 粒度、croniter、10 分钟容忍窗防久停风暴）；实测每分钟 cron 自动触发并回推 | 1.5 | M3-3 | ☑ 2026-08-13 |
| M3-5 | 技能管理完善 ☑：check-update（远端校验和对比）/ update（自动备份+重导入回 pending 重审）/ rollback（恢复备份+重扫描回 pending）；精选源浏览页留待有真实源清单时补 | 2 | M2-3 | ☑ 2026-08-13 |
| M3-6 | 用户级连接器授权起步：OAuth 流程 + user_credential 加密 + 按人注入（首期 2~3 个连接器）+ 管理页 | 3 | M2-10 | ☐ |
| M3-7 | 交付物评测集 ☑：eval/deliverable_eval.py 四格式 e2e（真实数据生成+程序化回读校验），首跑 **4/4** | 1.5 | M2-2 | ☑ 2026-08-13 |
| M3-8 | 生产部署 ☑：deploy/ 整栈 compose（pg/redis/aegra/gateway/webui）+ app 镜像（构建+自检通过，857MB）+ 部署手册（两形态+验证清单+运维要点） | 2 | 全部 | ☑ 2026-08-13 |
| M3-9 | 安全复查 ☑：docs/安全复查.md——密钥六项检查全过（新增审计凭据打码）、许可合规六组件清单、注入面五项评估；待办：对外部署前管理台加鉴权 | 1 | 全部 | ☑ 2026-08-13 |

**验收**：生产部署手册可复现；模板+定时演示；用户自助连邮箱演示；新评测基线归档。

---

## 关键决策点（需明确拍板后才能推进的任务）

| 决策 | 影响任务 | 时点 |
|---|---|---|
| Qwen 达标与否 → 混合路由方案 | M1-6 | M0-6 后 |
| Aegra 引入 vs 自建端点 | M1-7 | M0-7 后 |
| Docker 自研 vs Daytona | M3-1 | M2 复盘 |
| 技能审核策略（全审 vs 白名单源免审） | M2-4 | M2 前 |
