# 功能清单（Feature List）

> 版本：v1.0 · 2026-08-13
> 按系统模块组织，标注需求编号与交付里程碑。✦ = 自研件；其余为选件组装/配置。

## 1. 框架核心 Runtime（deepagents 组装层）

| 功能 | 需求 | 里程碑 |
|---|---|---|
| Lead Agent（规划 + todo + task 派单） | R1/R2 | M0 |
| 专家子代理 ×5（general / uav-ops / research / doc-writer / data-analyst），受限工具集 | R1 | M1 |
| 子代理定义 DB 配置化（改配置不改代码） | R1 | M1 |
| ✦ 审计中间件（wrap_tool_call → 审计表） | R11 | M1 |
| ✦ 模型路由中间件（档位策略 + 混合路由降级） | R8 | M1 |
| interrupt_on 高危工具拦截 | R5 | M0 |
| 上下文压缩（内置 summarization） | — | M1 |
| 跨会话记忆（/memories → PostgresStore） | R6 | M1 |
| Postgres checkpointer（断点恢复） | R9 | M1 |

## 2. MCP 集成层

| 功能 | 需求 | 里程碑 |
|---|---|---|
| MultiServerMCPClient 接入（streamable_http + X-API-Key headers） | R3 | M0 |
| 无人机 11 域全量接入（8201-8204、8206-8212） | R3 | M1 |
| ✦ MCP 配置生成器（Nacos 桥对接、热更新、IP 漂移自愈） | R3 | M1 |
| 工具发现失败重试（不缓存跳过）+ run 前 healthz 快检 | R3 | M1 |
| 工具分层挂载（Lead 高频域 + 子代理长尾域） | R3 | M1 |
| Playwright MCP（浏览器/网页研究） | R13 | M3 |
| ✦ 用户级连接器授权（OAuth + user_credential 加密 + 按人注入） | R19 | M3 |

## 3. Skills 层

| 功能 | 需求 | 里程碑 |
|---|---|---|
| Agent Skills 标准加载（skills= 目录，agentskills.io 规范） | R4 | M0 |
| 现有 8 个业务技能零改动迁移 | R4 | M1 |
| ✦ 自写文档四技能（pptx / docx / xlsx / pdf） | R4 | M2 |
| ✦ 技能管理：本地上传 zip 导入 | R15 | M2 |
| ✦ 技能管理：GitHub 地址导入（@tag/@sha 锁定、整仓扫描） | R15 | M2 |
| ✦ 技能管理：外部 URL / 内置精选源导入 | R15 | M2/M3 |
| ✦ 安全流水线（结构校验 + 静态扫描 + pending 人工审核） | R15 | M2 |
| ✦ 启停 / 作用域（global/tenant/user）/ 会话装配（新会话即时生效） | R15 | M2 |
| ✦ 检查更新 / 版本回滚 / pin 锁定 | R15 | M3 |
| 子代理按岗位限定技能子集 | R15 | M2 |

## 4. 知识库

| 功能 | 需求 | 里程碑 |
|---|---|---|
| ✦ 文档上传解析入库（PDF/Word/MD → 切分 → pgvector） | R16 | M2 |
| search_knowledge 检索工具（挂 Lead + 相关子代理） | R16 | M2 |
| ✦ 知识库管理页（列表/删除/重建索引/作用域） | R16 | M2 |

## 5. 文件系统与沙箱

| 功能 | 需求 | 里程碑 |
|---|---|---|
| LocalShellBackend（每会话受限目录 + execute，P0） | R4 | M2 |
| CompositeBackend 路由（/workspace 会话、/memories 持久） | R6 | M1 |
| ✦ Docker 沙箱后端（SandboxBackendProtocolV2 自研）或自托管 Daytona（P1） | R4 | M3 |

## 6. 人在环与审批

| 功能 | 需求 | 里程碑 |
|---|---|---|
| interrupt → 确认卡片 → 审批服务换 token → resume 闭环 | R5 | M2 |
| confirm_token 工具内防线（审批服务 8205，不动） | R5 | 既有 |
| 紧急白名单直通（服务端校验，不动） | R5 | 既有 |

## 7. 消息网关与服务端 ✦

| 功能 | 需求 | 里程碑 |
|---|---|---|
| LangGraph Server 协议端点（Aegra 裁剪或自建：threads/runs/stream/state/resume） | R10 | M1 |
| 后台 run worker（生命周期落库 + 重启扫描恢复） | R9 | M1 |
| Web SSE 流式接口 | R7 | M1 |
| 企业微信通道（webhook 验签、会话映射、分段消息、交互卡片） | R7 | M2 |
| 钉钉通道（同上） | R7 | M2 |
| 统一身份（渠道用户 → 身份表 → 身份链注入） | R11 | M2 |
| Playbook 模板 CRUD + 按模板发起 | R18 | M3 |
| 定时任务（cron 触发 + 回推） | R14 | M3 |

## 8. 工作台前端（agent-chat-ui 二开）

| 功能 | 需求 | 里程碑 |
|---|---|---|
| 对话流（流式、工具调用展示） | R10 | M1 |
| todo 计划面板 + 子代理活动流 | R2/R10 | M2 |
| 工作区文件浏览/下载 | R10 | M2 |
| 确认卡片（interrupt 渲染） | R5 | M2 |
| 技能管理页（列表/导入/预览/审核队列） | R15 | M2 |
| 知识库管理页 | R16 | M2 |
| 模板库页 | R18 | M3 |
| 连接器管理页（连接/断开） | R19 | M3 |

## 9. 运维与质量

| 功能 | 需求 | 里程碑 |
|---|---|---|
| Langfuse 自托管（trace + token/成本看板） | R17 | M1 |
| 审计 ↔ trace run_id 关联 | R11/R17 | M1 |
| 76 条评测集回归 runner | R12 | M1 |
| 交付物评测集 | R12 | M3 |
| 依赖矩阵锁定 + 升级回归流程 | N1 | M1 |
| 生产部署手册（compose） | — | M3 |
